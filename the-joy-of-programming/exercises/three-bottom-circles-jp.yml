answer: |-
  c1 = circle(x=0, y=25, r=25)
  c2 = circle(x=0, y=50, r=50)
  c3 = circle(x=0, y=75, r=75)

  # show the circles
  show(c1, c2, c3)
code: |
  # create the inner most circle
  c1 = circle(x=0, y=25, r=25)

  # create the remaining two circles

  # show the circles
description: >
  Draw three circles with bottom point of each circle touching at the origin. The three circles will have
  radius of 25, 50 and 75 respectively.
name: three-bottom-circles-jp
title: Three Circles touching at the Bottom
