answer: |-
  # create the center circle
  c1 = Circle(radius=50)

  # create the left and right circles
  c2 = Circle(center=Point(x=0, y=100), radius=50)
  c3 = Circle(center=Point(x=0, y=-100), radius=50)

  # show the circles
  show(c1, c2, c3)
code: |
  # create the center circle
  c1 = Circle(radius=50)

  # create the left and right circles


  # show the circles
description: 'Draw three circles in a column as shown in the figure. '
name: three-circles-in-a-column-jp
title: Three Circles in a Column
