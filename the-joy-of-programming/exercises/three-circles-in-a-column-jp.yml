answer: |-
  # create the center circle
  c1 = circle(x=0, y=0, r=50)

  # create the top and the bottom circles
  c2 = circle(x=0, y=-100, r=50)
  c3 = circle(x=0, y=100, r=50)

  # show the circles
  show(c1, c2, c3)
code: |
  # create the center circle
  c1 = circle(x=0, y=0, r=50)

  # create the top and the bottom


  # show the circles
description: 'Draw three circles in a column as shown in the figure.'
name: three-circles-in-a-column-jp
title: Three Circles in a Column
