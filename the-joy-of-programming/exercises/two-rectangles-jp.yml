answer: |-
  r1 = rectangle(w=200, h=100)
  r2 = rectangle(w=100, h=200)
  show(r1, r2)
code: |-
  # the first rectangle
  r1 = rectangle(w=200, h=100)

  # create second rectangle r2


  # change this to show both the shapes
  show(r1)
description: Draw two rectangles as shown in the figure. Both the rectangles have
  sides as 100 and 200.
name: two-rectangles-jp
title: Two Rectangles
