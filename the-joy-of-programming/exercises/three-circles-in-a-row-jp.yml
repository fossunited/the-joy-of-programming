answer: |-
  # create the center circle
  c1 = circle(x=0, y=0, r=50)

  # create the left and right circles
  c2 = circle(x=-100, y=0, r=50)
  c3 = circle(x=100, y=0, r=50)

  # show the circles
  show(c1, c2, c3)
code: |
  # create the center circle
  c1 = circle(x=0, y=0, r=50)

  # create the left and right circles


  # show the circles
description: 'Draw three circles in a row as shown in the figure.'
name: three-circles-in-a-row-jp
title: Three Circles in a Row
