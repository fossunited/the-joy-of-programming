answer: |-
  s1 = ellipse(w=200, h=100)
  s2 = ellipse(w=100, h=200)
  show(s1, s2)
code: |-
  # the first ellipse
  s1 = ellipse(w=200, h=100)

  # create second ellipse


  # change this to show both the shapes
  show(s1)
description: Draw two ellipses as shown in the figure.
name: two-ellipses-jp
title: Two Ellipses
